#include<stdio.h>

int main()
{
    int soma = 0, contador = 0;
    float salario;
    

    while (contador < 10)
    {
        printf("Digite seu salario: ");
        scanf("%f", &salario);

        soma = soma + idade;
        contador++;
    }

    media = (float)soma / contador;

    printf("Media das idades: %.2f\n", media);

    return 0;
}